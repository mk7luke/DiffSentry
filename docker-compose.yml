services:
  diffsentry:
    build: .
    ports:
      - "${PORT:-3005}:3005"
    env_file:
      - .env
    volumes:
      - ./private-key.pem:/app/private-key.pem:ro
      - diffsentry-data:/app/data
    restart: unless-stopped

volumes:
  diffsentry-data:
